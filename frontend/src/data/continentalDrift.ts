/**
 * Continental drift data — re-exported from mockLayers.ts for backward compatibility.
 */
export {
  type DriftKeyframe,
  type ContinentDriftData,
  type GeoEra,
  DRIFT_MAX_MA,
  DRIFT_MIN_MA,
  CONTINENT_DRIFT,
  GEO_ERAS,
  getEraForTime,
  interpolateContinents,
} from './mockLayers';
